extraction:
  cpp:
    before_index:
      - pip3 install numpy==1.17.3 scipy==1.3.2 cython==0.29.14
    index:
      build_command:
        # Compile the Cython extension modules
        # to analyze the generated C++ sources
        - python3 setup.py build_ext -i
