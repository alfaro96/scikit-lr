# Configuration file with simple models

- include: base.yml

- config: dummy
  description: Base object for simple models
  extends: base
  flags:
    #
    strategy:
      description: The strategy to use to generate predictions
      type: string
      default: most_frequent
      choices: [most_frequent]
